{
  "name": "squared-help-center",
  "version": "7.0.0",
  "private": true,
  "description": "Squared Help Center — production-ready static-first support platform with React islands, Tailwind CSS and Supabase.",
  "scripts": {
    "build:tailwind": "npx @tailwindcss/cli -i ./src/tailwind.css -o ./assets/tailwind.generated.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
