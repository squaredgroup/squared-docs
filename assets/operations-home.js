import {db,checked,aggregateState,labels,esc,href} from './hc-core.js';
const host=document.querySelector('.home-status-list');
async function update(){if(!host)return;try{const components=checked(await db.from('service_components').select('id,name,product,enabled,public,aggregate_status,lifecycle,monitor_type,current_status,last_checked_at').eq('enabled',true).eq('public',true));const groups=new Map();for(const c of components){if(!groups.has(c.product))groups.set(c.product,[]);groups.get(c.product).push(c);}host.innerHTML=[...groups].map(([name,list])=>{const s=aggregateState(list);return '<div class="home-status-row"><span>'+esc(name)+'</span><a href="'+href('server-status.html#'+list[0].id)+'" style="color:var(--muted);text-decoration:none">'+esc(labels[s])+'</a></div>';}).join('');const orb=host.closest('.home-status-card')?.querySelector('.status-orb');if(orb)orb.style.display='none';}catch{host.innerHTML='<p class="ops-muted">Mesures indisponibles. <a href="'+href('status.html')+'">Consulter lՎtat des services</a>.</p>';}}
update();
